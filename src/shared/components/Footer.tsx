import React from 'react'
import { Box, Typography } from '@mui/material';

const Footer = () => {
    return (
        <Box className="bg-black-alpha-50" sx={{height: "100px"}}>
            <Typography>
                Footer
            </Typography>
        </Box>
    );
};

export default Footer;